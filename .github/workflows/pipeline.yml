name: ci

on:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bump version from conventional commits
        id: tag
        uses: mathieudutour/github-tag-action@v6.2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          default_bump: false
          release_branches: main

      - name: Exit if no release
        if: ${{ !steps.tag.outputs.new_tag }}
        run: |
          echo "No conventional commit triggered a version bump — skipping build and release."
          exit 0

      - name: Set up JDK 21
        if: steps.tag.outputs.new_tag
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Verify generated data
        if: steps.tag.outputs.new_tag
        run: ./gradlew checkGeneratedData -PreleaseVersion=${{ steps.tag.outputs.new_tag }}

      - name: Build
        if: steps.tag.outputs.new_tag
        run: ./gradlew build -PreleaseVersion=${{ steps.tag.outputs.new_tag }}

      - name: Create GitHub release
        if: steps.tag.outputs.new_tag
        uses: ncipollo/release-action@v1
        with:
          artifacts: build/libs/fossilsarch2-*.jar
          tag: ${{ steps.tag.outputs.new_tag }}
          name: ${{ steps.tag.outputs.new_tag }}
          body: ${{ steps.tag.outputs.changelog }}
